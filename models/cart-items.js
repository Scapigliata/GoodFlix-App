class CartIItem {
  constructor(movieTitle, movieRating, movieContent) {
    this.movieRating = movieRating
    this.movieTitle = movieTitle
    this.movieContent = movieContent
  }
}

export default CartIItem;