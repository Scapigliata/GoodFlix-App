class Movie {
  constructor(id, userId, title, imageUrl, content, rating) {
    this.id = id
    this.userId = userId
    this.title = title
    this.imageUrl = imageUrl
    this.content = content
    this.rating = rating
  }
}

export default Movie